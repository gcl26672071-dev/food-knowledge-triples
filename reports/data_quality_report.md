# Data Quality Report / 数据质量报告

生成日期：2026-09-11

## 1. 总体统计

- 原始数据：**3625** 行。
- 完整三元组：**3539** 行。
- 精确去重后三元组：**2032** 行。
- 完整记录中的额外重复行：**1507** 行。
- 存在重复的三元组组：**957** 组。
- 不完整记录：**86** 行。

## 2. 不完整记录分类

- `subject`、`relation` 为空，仅 `object/value` 有值：**81** 行，对应 **49** 个不同值。
- `subject`、`relation` 有值但 `object` 为空：**4** 行。
- 三个字段均为空：**1** 行。

这些记录没有被强行修复，已全部放入 `data_review/`。

## 3. 关系分布

### 完整记录（含重复）

| relation | 数量 |
|---|---:|
| 名称 | 584 |
| 外形 | 1328 |
| 口感 | 978 |
| 味道 | 649 |

### 精确去重后

| relation | 数量 |
|---|---:|
| 名称 | 422 |
| 外形 | 672 |
| 口感 | 570 |
| 味道 | 368 |

## 4. 实体/节点规模（基于去重版）

- 唯一 `subject`：**422**
- 唯一 `object`：**946**
- `subject ∪ object` 唯一节点：**947**

## 5. 本次未自动处理的问题

本次整理不进行语义级自动修复，因此仍需注意：

- 某些 `object` 可能在语义上更像另一种属性或实体名称；
- 同义词、近义词和表述变体没有合并；
- 重复记录可能代表源数据中的频次，也可能是采集重复，因此同时提供含重复版和去重版；
- 不完整记录无法可靠推断缺失关系，未自动补全；
- 数据来源与第三方许可情况无法仅凭 CSV 内容判断，需要公开前人工确认。

## 6. 推荐使用方式

- 知识图谱构建、三元组检索、关系建模：优先使用 `data/processed/food_triples.csv`。
- 如果实验需要保留原始重复频次：使用 `data/processed/food_triples_all_complete.csv`。
- 做数据修订或追溯：结合 `data/raw/` 与 `data_review/`。
